import { environment } from '../../../environments/environment';

export const APP_NAME = environment.appName;
export const APP_VERSION = environment.appVersion;
export const API_BASE_PATH = environment.apiBasePath;
export const API_BASE_URL = environment.apiBaseUrl;
export const SOCKET_URL = environment.socketUrl;
export const AGENT_DOWNLOAD_URL = environment.agent.downloadUrl;
export const AGENT_VERSION = environment.agent.version;
